# ÉCLAT 环境变量配置
# 复制本文件为 .env 并填入你的配置值
# 注意：.env 文件不应提交到版本控制

# ===== AI 聊天功能（可选）=====
# 如果不需要 AI 聊天功能，可留空，系统会自动降级为预设回复
# 获取方式：腾讯云 AI Gateway 或其他 OpenAI 兼容接口
VITE_AI_API_KEY=

# ===== EdgeOne 边缘函数（可选）=====
# 本地开发模式下无需配置，推荐系统会自动降级为本地规则推荐
# 部署到 EdgeOne Pages 后，在 EdgeOne 控制台设置环境变量：
# AI_API_KEY = 你的 AI 接口密钥（用于 edge-functions/api/ai-chat.js）

# ===== 管理员接口密钥（必填，强烈建议设置）=====
# 用于保护 /api/admin/* 接口（如产品数据初始化 init-products）。
# 调用时需携带：Authorization: Bearer <该值>
# 必须是一段足够随机的长字符串（建议 32+ 字符），不要使用简单词；
# 该值只存于 EdgeOne 环境变量，切勿提交到代码库。
# 未配置时，所有 /api/admin/* 请求一律被拒绝（fail-closed）。
ADMIN_SECRET=
